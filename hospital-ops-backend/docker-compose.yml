version: '3.8'
services:
  inventory-agent:
    build: ./inventory_agent
    ports:
      - "8081:8080"
  vision-agent:
    build: ./vision_agent
    ports:
      - "8082:8080"
    environment:
      - INVENTORY_AGENT_URL=http://inventory-agent:8080